{
  "name": "egy-discovery",
  "version": "1.0.0",
  "description": "Business Intelligence Platform",
  "scripts": {
    "build": "npm ci && npm run build",
    "dev": "npm run dev",
    "start": "python server/main.py"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}